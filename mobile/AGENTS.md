# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(Pinned to v54 because that's the newest SDK Expo Go supports on iOS as of 2026-07-17 — check
`node_modules/expo/bundledNativeModules.json` for the exact dependency versions actually installed
before bumping this, and re-verify Expo Go's supported SDK before upgrading past it.)
